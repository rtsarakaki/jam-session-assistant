/**
 * Stored in `profiles.instruments` alongside instrument presets.
 * Jam scoring: selected participant counts for every song; if they also have the song in repertoire, one extra weight (audience-style emphasis).
 */
export const PROFILE_JAM_PLAYS_ANY_SONG = "Any song (full repertoire)" as const;
